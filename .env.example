# Copy this file to .env and adjust values for your local setup.
# .env is intentionally excluded from git.

ENV=dev
BROWSER=chrome
HEADLESS=true
# Leave BASE_URL empty to resolve from environments.json using the ENV value above.
# Uncomment to hard-override for a one-off run:
# BASE_URL=https://custom.example.com
BASE_URL=
